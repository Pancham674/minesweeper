@model Cell

<button class="cell"
    data-model='@Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model))'>
        @(
            Model.IsExploded ? "💥" :
                !Model.IsRevealed && Model.IsBomb ? "💣" :
                    Model.IsRevealed ? Model.NeighboringBombs : ""
        )
</button>